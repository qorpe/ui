---
"@qorpe/ui": patch
---

Sheet gains `maxWidth` (default 680): adopter feedback from mockifyr M2 —
wide journal-detail panels (720) and narrow behavior panels (480) are real
anatomies; the ceiling is a number, not a class-conflict fight.
